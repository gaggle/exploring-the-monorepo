{
  "name": "@mono/types",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "docker:build":"tar --exclude='node_modules' --exclude='dist' --exclude='.git' -cf - ../.. | docker build -f libs/types/Dockerfile -"
  },
  "dependencies": {
    "type-fest": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.41",
    "typescript": "^4.2.4"
  },
  "private": true
}
