{
  "name": "exploring-the-monorepo",
  "description": "Exploring The Monorepo (a practical, hands-on Typescript guide)",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "clean": "rm -rf node_modules apps/web/tsconfig.tsbuildinfo libs/types/tsconfig.tsbuildinfo package-lock.json apps/web/build apps/web/cache apps/web/node_modules libs/types/node_modules apps/web/dist libs/types/dist",
    "docker:build": "pnpm -r --if-present --parallel docker:build",
    "pnpm-context": "bin/pnpm-context.mjs",
    "list-packages": "echo [$(pnpm -s m ls --depth -1 | tr \" \" \"\n\" | grep -o \"@.*@\" | rev | cut -c 2- | rev | sed -e 's/\\(.*\\)/\"\\1\"/' | paste -sd, - )]",
    "preinstall": "bin/preinstall",
    "start": "pnpm -r --if-present --parallel start"
  },
  "devDependencies": {
    "@pnpm/filter-workspace-packages": "^4.1.11",
    "@pnpm/logger": "^4.0.0",
    "globby": "^12.0.2",
    "meow": "^10.1.1",
    "mississippi": "^4.0.0",
    "tar": "^6.1.11",
    "typescript": "4.3.3"
  },
  "engines": {
    "node": ">= 16.1.0",
    "pnpm": ">= 6.9.0"
  },
  "private": true
}
